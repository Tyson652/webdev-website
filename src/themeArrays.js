export const Sizes = () => {
  let array = [];
  for (let i = 1; i <= 2000; i += 0.01 * 100) {
    array.push(i);
  }
  return array
};

export const Space = () => {
  let array = [];
  for (let i = 1; i <= 1000; i += 0.01 * 100) {
    array.push(i);
  }
  return array
};

export const FontSizes = () => {
  let array = [];
  for (let i = 1; i <= 100; i += 0.01 * 100) {
    array.push(i);
  }
  return array
};

export const BorderWidths = () => {
  let array = [];
  for (let i = 1; i <= 100; i += 0.01 * 100) {
    array.push(i);
  }
  return array
};

export const Radii = () => {
  let array = [];
  for (let i = 1; i <= 100; i += 0.01 * 100) {
    array.push(i);
  }
  return array
};